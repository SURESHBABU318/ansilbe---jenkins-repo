---
- hosts: webservers 
  become: True
  tasks:
    - name: install apache2
      apt: name=apache2 update_cache=yes state=latest
    - name: Deploy static website
      copy:
        src: index.html
        dest: /var/www/html/
...
